import React, { Component } from "react";
import cs from "classnames";

class List extends Component {
  constructor(props) {
    super(props);
    console.log(props);
  }
  render() {
    return <div />;
  }
}

export default List;
